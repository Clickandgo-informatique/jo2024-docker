<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
	</head>
	<body style="font-family:sans-serif; font-size:12px; text-align:center;">

		<div style="display:inline-block; border:1px solid #ccc; padding:15px; margin:20px auto; width:300px; text-align:left;">
			<div style="margin-bottom:5px;">
				<strong>Commande :</strong>
				{{ ticket.commande.reference }}</div>
			<div style="margin-bottom:5px;">
				<strong>Payée le :</strong>
				{{ ticket.commande.payeeLe|date('d/m/Y H:i') }}</div>

			{% if ticket.qrCodePath %}
				<div style="text-align:center; margin:10px 0;">
					{{ ticket.qrCodePath|raw }}
				</div>
			{% endif %}

			<div style="margin-bottom:5px;">
				<strong>Clé :</strong>
				{{ ticket.ticketKey }}</div>
			<div style="margin-bottom:5px;">
				<strong>Hash :</strong>
				{{ ticket.payloadHash }}</div>
		</div>

	</body>
</html>
