{% extends 'base.html.twig' %}
{% block title %}
	{{title}}
{% endblock %}
{% block body %}

	<a href="{{path('app_utilisateurs_index')}}" class="btn btn-success">
		<i class="fa-solid fa-list"></i>Revenir à la liste</a>
	{{ form_start(form) }}
	<caption>
		<h1 class="text-center">
			<i class="fa-solid fa-user"></i>
			{{title}}</h1>
	</caption>
	{{ form_row(form.nickname) }}
	{{ form_row(form.email) }}
	{{ form_row(form.roles) }}
	<button type="submit" class="btn btn-primary">
		<i class="fa-solid fa-floppy-disk"></i>Enregistrer</button>
	{{ form_end(form) }}

{% endblock %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{asset('assets/css/form.css')}}">
{% endblock %}
